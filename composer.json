{
    "name": "bazy/coaching-app",
    "description": "Coaching Management Platform",
    "type": "project",
    "require": {
        "php": "^8.4"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "@php -r \"if (!file_exists('config')) mkdir('config');\""
        ]
    }
}
